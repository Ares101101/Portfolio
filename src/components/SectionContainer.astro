---
const { class: className, id } = Astro.props
---
<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto lg:w-[1080px] grid justify-center items-center`}
>
  <slot />
</section>